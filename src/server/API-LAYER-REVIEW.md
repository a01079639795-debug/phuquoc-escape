# Транспорт, хранилище и уведомления — материалы для ревью

**Дата:** 26.08.2026
**Статус:** ожидает ревью. UI не создавался.
**Предыдущие этапы:** [схема данных](../../prisma/SCHEMA-REVIEW.md) (утв. 25.08) · [service layer](./SERVICE-LAYER-REVIEW.md) (утв. 26.08)

---

## 1. Что сделано

Три пункта в заданном порядке: транспортный слой Next.js поверх готовых сервисов, адаптер объектного хранилища с presigned-загрузкой и проверкой содержимого, уведомления в Telegram.

| Слой | Файлов | Строк |
|---|---|---|
| `src/app/api/v1/**` — route handlers | 44 | 589 |
| `src/server/http` — ядро транспорта | 4 | 339 |
| `src/server/storage` — порт и адаптеры | 4 | 283 |
| `src/server/notifications` — порт и каналы | 4 | 278 |

**Не создавалось по договорённости:** UI, `Booking`, `Payment`, CRM, электронная почта.

Бизнес-логика из service layer не переносилась: транспорт только разбирает вход и вызывает сервис.

---

## 2. Два найденных дефекта

### 2.1. Коды ошибок собирались из русских названий

`NotFoundError` строил код как `${entity.toUpperCase()}_NOT_FOUND`, где `entity` — человекочитаемое название. Наружу уходило **`ОБЪЕКТ_NOT_FOUND`**.

Код ошибки — машинный идентификатор: по нему клиент ветвит поведение, по нему группируются логи. Кириллица делает его непригодным, и вдобавок он менялся бы при первой правке формулировки.

Заменено типизированным справочником: код и текст разделены, ключи проверяет компилятор.

```ts
const ENTITIES = {
  listing: { code: 'LISTING', message: 'Объект не найден' },
  request: { code: 'REQUEST', message: 'Заявка не найдена' },
  …
} as const;
```

Дефект нашли тесты транспорта — на уровне сервисов он был не виден, потому что там ошибку ловят по классу, а не по коду.

### 2.2. Снятие EXIF ломало разбор размеров

Первая реализация вырезала из JPEG **все** блоки `APPn`. Файл остаётся формально валидным, но без заголовка JFIF строгие анализаторы перестают читать размеры.

Интеграционный тест этого не поймал: размеры снимаются с исходных байтов **до** очистки. Поймал модульный тест на самой функции.

Теперь вырезаются `APP1` (EXIF с геометкой и XMP), `APP3`–`APP15` и комментарии. `APP0` (JFIF) и `APP2` (цветовой профиль ICC) сохраняются: первый структурный, второй отвечает за цветопередачу. Если APP-заголовка не осталось — подставляется минимальный JFIF.

---

## 3. Транспорт: обработчики остаются тонкими

Типичный обработчик 3–8 строк, самый длинный 21. Целиком:

```ts
import { handle } from '@/server/http/handler';
import { catalog } from '@/server';

export const GET = handle<{ slug: string }>(async (ctx) =>
  catalog.getListingBySlug(ctx.params.slug, ctx.locale),
);
```

Всё общее живёт в обёртке `handle()`: разбор cookie сессии, проверка CSRF, разбор параметров пути и строки запроса, единый конверт ответа, перевод доменных ошибок.

Обработчики генерируются из таблицы в `scripts/generate-routes.mjs`. Причина не в экономии: сорок с лишним почти одинаковых файлов невозможно проверить глазами на предмет просочившейся логики, а одну таблицу — можно.

### Разбор строки запроса

Повторяющиеся ключи собираются в массив (`?amenities=pool&amenities=spa`), `true`/`false` приводятся к булеву типу. Числа не трогаются транспортом — их приводят схемы сервисов через `z.coerce`, поэтому одно и то же поле одинаково работает и в строке запроса, и в JSON-теле.

---

## 4. Сессия и CSRF

| Cookie | Флаги | Зачем |
|---|---|---|
| `pq_session` | `HttpOnly`, `SameSite=Lax`, `Secure` в проде | Токен недоступен скриптам: XSS не даёт его украсть |
| `pq_csrf` | без `HttpOnly` | Клиент обязан прочитать и вернуть заголовком — в этом суть double submit |

Токен сессии не попадает в тело ответа: он живёт только в cookie.

**Проверка CSRF применяется к изменяющим запросам от аутентифицированного пользователя.** CSRF эксплуатирует попутные полномочия — cookie, которую браузер отправляет автоматически. Без сессии подделывать нечего, а у гостя ещё нет токена, который можно было бы вернуть.

| Запрос | Проверка |
|---|---|
| `GET` с сессией | не требуется |
| `PATCH /me` с сессией | **обязательна** |
| `POST /requests` от гостя | не требуется — попутных полномочий нет |
| `POST /auth/login` | не требуется — сессии ещё нет |

Для гостевых POST основной защитой остаётся `SameSite=Lax`.

---

## 5. Перевод ошибок в коды состояния

Сервисы не знают про HTTP. Перевод происходит в одном файле — `src/server/http/respond.ts`.

| Источник | Код | Тело |
|---|---|---|
| `ValidationError` | 422 | код + разбор по полям |
| `UnauthorizedError` | 401 | |
| `ForbiddenError` | 403 | |
| `NotFoundError` | 404 | `LISTING_NOT_FOUND` и т. п. |
| `ConflictError` | 409 | |
| `TooManyRequestsError` | 429 | |
| Prisma `P2002` | 409 | обобщённое сообщение |
| Prisma `P2023` (неверный UUID) | 400 | ошибка клиента, не наш сбой |
| Всё остальное | 500 | **только код**, без текста |

Текст неизвестной ошибки наружу не отдаётся: сообщения Prisma содержат имена таблиц, пути и фрагменты запросов.

---

## 6. Хранилище

`StoragePort` и три реализации: `S3Storage` (Cloudflare R2 в проде, MinIO локально — протокол один), `MemoryStorage` (тесты), выбор по переменным окружения. В продакшене без настроек приложение падает при старте; вне продакшена — предупреждение и память. Молчаливый переход на заглушку был бы хуже явного отказа.

### Поток загрузки

```
1. Админка просит ссылку
     POST /admin/media/upload-url  { mime, sizeBytes }
       └─ сервер проверяет тип и размер, генерирует ключ (UUID),
          возвращает presigned PUT

2. Браузер кладёт файл НАПРЯМУЮ в R2 или MinIO
     (через наш сервер файл не идёт: нет нагрузки и нет лимитов body-parser)

3. Админка подтверждает
     POST /admin/media/confirm  { storageKey }
       └─ и вот здесь мы впервые видим содержимое
```

### Что проверяется при подтверждении

| Проверка | Поведение |
|---|---|
| Тип по сигнатуре байтов | JPEG / PNG / WebP / AVIF. SVG в списке отсутствует намеренно — это XML со скриптом |
| Заявленный тип ≠ фактический | В базу пишется **фактический** |
| Не изображение | Отказ **и удаление объекта** из хранилища |
| Размеры | Читаются из содержимого, а не со слов клиента |
| EXIF | Вырезается, см. 2.2 |
| Размер | Сверяется с фактическим, а не с обещанным |

Ключ объекта генерирует только сервер: имя файла от клиента открыло бы путь к перезаписи чужих объектов и path traversal.

---

## 7. Уведомления

`NotifierPort` и три реализации: `TelegramNotifier` (Bot API через `fetch`), `ConsoleNotifier` (без настроек пишет в лог вместо отправки), `MemoryNotifier` (тесты).

**Два правила:**

- сбой канала не роняет заявку — она уже сохранена, и недоступный Telegram не повод отвечать клиенту ошибкой;
- отправка не блокирует ответ — уходит в фон.

Из-за второго правила добавлен `flushNotifications()`: иначе фоновую отправку невозможно проверить детерминированно.

Повтор с тем же ключом идемпотентности до отправки не доходит — дубля сообщения менеджеру не будет.

**Электронная почта не реализована** по указанию. Добавляется новой реализацией того же интерфейса.

### О персональных данных

Сообщение содержит контакты клиента — это рабочий инструмент: менеджер перезванивает, не открывая админку. Обратная сторона — персональные данные уходят во внешний сервис, поэтому чат обязан быть закрытым, а состав участников контролируемым. Отмечено в `.env.example` рядом с переменными.

Пользовательский текст экранируется: `<script>` в комментарии к заявке не станет разметкой.

---

## 8. Проверки

| Набор | Команда | Результат |
|---|---|---|
| Типизация | `npm run typecheck` | чисто, строгий режим |
| Сборка Next | `npm run build` | 44 маршрута |
| Инварианты схемы | `npm run db:verify` | 16 / 16 |
| Гонка обложки | `npm run db:verify:race` | 4 / 4 |
| Сид | `npm run db:verify:seed` | 27 / 27 |
| Service layer | `npm test` | 53 / 53 |
| **Транспорт** | `npm run test:http` | **30 / 30** |
| **Хранилище и медиа** | `npm run test:media` | **19 / 19** |
| **Уведомления** | `npm run test:notify` | **11 / 11** |

**Всего 160 проверок, все пройдены.** Сводно: `npm run verify:all`

### Что покрывают новые тесты

**Транспорт (30):** конверт ответа и метаданные постраничной выдачи; приведение чисел из строки запроса; потолок `perPage` не обходится; массивы в параметрах; httpOnly и SameSite на сессии; токен не попадает в тело; выход обрывает сессию; CSRF без заголовка и с неверным заголовком; гостевой POST без токена; 401/403/404/409/422/429; неверный UUID даёт 400; битый JSON не роняет процесс; идемпотентность через заголовок.

**Хранилище (19):** распознавание четырёх форматов; SVG и исполняемый файл не проходят; размеры из содержимого; вырезание EXIF с сохранением читаемости; ключ генерирует сервер; подменённый тип; удаление мусора из хранилища; конфликт при повторном подтверждении; запись в журнал аудита; привязка к объекту с сохранением единственной обложки.

**Уведомления (11):** состав сообщения; экранирование разметки; отсутствие пустых строк; отправка при создании заявки; сбой канала не мешает принять заявку; отсутствие дубля при повторе; отсутствие уведомления при отклонённой заявке; недоступная сеть и ошибка Telegram не приводят к исключению; корректный вызов `sendMessage`.

---

## 9. Известные ограничения

| Ограничение | Когда снимается |
|---|---|
| Счётчики частоты в памяти процесса — обнуляются при перезапуске, не общие для нескольких инстансов | при переходе на несколько инстансов, переносом в Redis |
| `blurDataUrl` — нейтральная заглушка: настоящее размытие требует перекодирования | вместе с обработкой изображений (sharp) |
| Производные размеры изображений не генерируются; раздача идёт через оптимизацию Next | при росте нагрузки — imgproxy или Cloudflare Images |
| Уведомления не переотправляются при сбое канала | при появлении очереди задач |
| Электронная почта отсутствует | по указанию, следующий этап |
| `npm run verify:all` последовательно поднимает пять серверов и потому долгий | при желании — общий сервер на все наборы |

---

## 10. Рекомендуемые следующие шаги

1. **UI** — публичный каталог, кабинет и админка поверх готового API. Транспорт зафиксирован, переделок не потребует.
2. **Электронная почта** — подтверждение заявки клиенту и письма сброса пароля. Сейчас токен сброса возвращается сервисом, но отправлять его некому.
3. **MinIO в docker-compose** — чтобы загрузка работала локально из коробки, без внешнего аккаунта.
4. **Развёртывание демо** — Vercel, управляемый Postgres, R2.

Первым пунктом идёт UI: остальное — инфраструктура, которую удобнее настраивать, когда есть что показывать.
